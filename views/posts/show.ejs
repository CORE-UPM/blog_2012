<p>
	<b><%= post.title %></b>
	<br />
	<em><%= post.author && post.author.name || 'Anónimo' %></em>
</p>
<p><%= post.updatedAt.toLocaleDateString() %></p>
<p><%- escapeText(post.body) %></p>
<% if (session.usr && session.user.id == post.authorId) { %>
	<a href="/posts/<%= post.id %>/edit">Editar</a>
<% } %>
<a href="/posts">Volver</a>
